{
  "name": "elemia",
  "private": true,
  "version": "0.0.0",
  "description": "Elemia monorepo — runtime-agnostic typed BEM class generation framework",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
